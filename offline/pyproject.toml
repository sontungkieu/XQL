[project]
name = "xql-offline" 
version = "0.1.0"
description = "Project generated from pinned requirements"
readme = "README.md"
authors = [{ name = "Your Name" }]
license = "MIT"
requires-python = ">=3.8,<3.10"

dependencies = [
    "numpy >= 1.20.2,<1.24.4",
    "scipy >= 1.6.0,<1.11.1",
    "absl-py >= 0.12.0",
    'gym[mujoco-py] <= 0.24.1',
    "gdown >= 3.12.2",
    "tqdm >= 4.60.0",
    "flax >= 0.3.5",
    "jax <=0.3.15",
    "jaxlib <= 0.3.15",
    "ml_collections >= 0.1.0",
    "optax >= 0.0.6,<0.1.9",
    "tensorboard < 2.15",
    "tensorflow-probability >= 0.14.1",
    "imageio >= 2.9.0",
    "imageio-ffmpeg >= 0.4.3",
    "d4rl @ git+https://github.com/rail-berkeley/d4rl@master#egg=d4rl",
    "wandb<0.19.12",
    "tensorboardX",
]

# Removed [build-system] to skip editable install/build; uv sync will only install dependencies.
# For local imports (e.g., from actor.py or wrappers/), use `uv run python script.py` or add current dir to PYTHONPATH.
# If you need full editable package later, add back [build-system] and configure below:
# [tool.setuptools.packages.find]
# include = ["offline*"]  # Assume main package is 'offline'; adjust if needed
# exclude = ["configs*", "reproduce*"]  # Exclude non-code dirs; include "wrappers*" if importing from it